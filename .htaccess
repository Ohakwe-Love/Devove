RewriteEngine On

# Let static files and folders pass
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Exclude admin routes so they go to the admin router
RewriteCond %{REQUEST_URI} !^/admin/

# Everything else routes to main site
RewriteRule ^ index.php [L]
